{
    "name": "peeps-web-service",
    "version": "0.0.0",
    "private": true,
    "extends": "../../package.json",
    "scripts": {
        "start": "ts-node src/index.ts",
        "start:watch": "nodemon src/index.ts --watch",
        "build:compile": "npx babel src --extensions .ts --out-dir build --source-maps",
        "build:types": "tsc",
        "deploy": "ansible-playbook deploy_app.yml",
        "print_token": "gcloud auth print-identity-token",
        "typeorm": "typeorm-ts-node-commonjs"
    },
    "dependencies": {
        "colormaker": "1.0.4"
    }
}
